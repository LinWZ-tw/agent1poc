# Core LLM SDKs
anthropic>=0.111.0
openai>=1.0.0

# Report figures (no display needed — uses Agg backend)
matplotlib>=3.7.0

# scRNA-seq steps and demo data download
scanpy>=1.9.0
anndata>=0.9.0
h5py>=3.8.0
